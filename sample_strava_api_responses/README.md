Example responses from Strava API endpoints. The base Strava API is `https://www.strava.com/api/v3`. The subdirectories here correspond to endpoints. JSON files within the subdirectories contain sample responses.

Note: some long fields are omitted from the sample responses. Omitted fields will be annotated, e.g., "=== LIST OMITTED ===".
